-- ################################################## 高亮配色 ##################################################
local nvim = vim

-- ================================================== 设置配色 ==================================================
-- 更改TelescopePromptCounter样式(原设置为 NonText, 导致查询条目不显示)
nvim.api.nvim_set_hl(0, 'TelescopePromptCounter', { default = true, link = "Comment" })
